# v101 — 신청서·증빙자료 최대 10개 첨부

## 바뀐 내용
- 파일 선택을 여러 번 해도 앞서 고른 목록에 누적합니다. PDF·한글·워드·사진·ZIP을 섞어 최대 10개까지 추가하고 개별 삭제할 수 있습니다.
- 총 용량은 기존 서버 한도에 맞춰 10MB 이하입니다. 전송 정보 때문에 한도에 아주 가까운 묶음은 용량을 조금 줄여야 할 수 있습니다.
- 사진을 임의로 축소하거나 합치지 않고, Google Drive의 해당 프로그램 ‘신청서 모음’에 원본 파일을 각각 저장합니다. 파일명에는 신청자·접수번호·순번이 붙어 중복 이름도 구분됩니다.
- 일반 ZIP 파일은 이용자가 올린 원본 파일 하나로 유지합니다.
- 파일 추가/삭제 상태를 화면낭독기에 알리고 키보드 삭제 후 초점을 유지합니다. 새 신청을 열면 이전 첨부는 초기화됩니다.
- v100 검색 안내·동의 그룹·설문 질문명·단계 이동 초점과 기존 등대/사이트 제목을 유지했습니다.

## 적용 순서 (운영 미배포)
1. 기존 Apps Script 프로젝트의 전체 코드를 google-apps-script-v101.gs로 교체합니다. 저장 후 기존 웹 앱 배포를 ‘새 버전’으로 갱신합니다. URL·WEBHOOK_SECRET·권한 설정을 유지합니다.
2. 이후 Netlify에 v101 ZIP을 배포합니다. 순서를 바꾸면 이전 Apps Script는 여러 첨부를 전송 묶음 하나로 저장할 수 있습니다.
3. 신청서+증빙자료를 각각 추가해 제출하고 해당 프로그램 폴더에 원본들이 따로 있는지 확인합니다.
Supabase 함수·SQL·DB 구조 변경은 없습니다. 이번 요청에서는 수정 파일과 GitHub·공개 소스 갱신까지 수행하며 운영 배포는 수행하지 않습니다.

## 내부 호환 방식
기존 단일 파일 서버 인터페이스를 유지하기 위해 여러 파일은 브라우저에서 무압축 전송 묶음으로 보냅니다. Apps Script가 예약된 v101 묶음만 개수·용량·형식·체크섬 검증 후 각 파일로 저장합니다. 최종 Google 자료로 전송 묶음을 추가 저장하지 않습니다. 관리자 화면은 여러 파일의 경우 파일 모음 폴더를 안내합니다. 한 파일 업로드 및 과거 자료 조회는 유지합니다.
기존 첨부형 경로는 Google 저장 성공 후 임시 Storage 파일 삭제 동작을 그대로 사용합니다. 설문형 경로는 파일을 Google로 전달합니다. 개인정보 동의 보관 기준은 변경하지 않았습니다.

## 검증
- 운영 소스 Node 회귀 검사 13개 통과.
- 공개판 Node 검사 13개 통과: 신규 스키마·RLS·권한·개인정보 저장·설문 기능 포함.
- 10개 혼합 파일의 원본 바이트 일치, 독립 저장, 재시도 중복 방지, 11개/용량 초과/손상 묶음 차단.
- 브라우저: 나누어 추가, 삭제·초점, 10개 한도, 오류 후 기존 목록 유지, 새 신청 초기화.
- v100 접근성 브라우저 검사(운영 소스·공개판) 및 기존 v94 신청·관리·모바일 회귀 검사 통과.
- 공개 소스 비밀설정 패턴 검사, 빌드·함수 준비 통과.
외부 API는 모의 응답으로 검사했습니다. 실제 신청자 데이터 저장·운영 배포·실제 NVDA 음성 검증은 이번 작업에 포함하지 않습니다.
