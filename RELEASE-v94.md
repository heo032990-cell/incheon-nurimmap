# v94 — 기관명·설문 선택 규칙·신청 단계 개선

- 일반 담당자의 기관명은 로그인 계정의 등록 기관으로 표시하며, 새 등록·초기화·복사·수정 후에도 유지합니다. 최고관리자는 직접 입력할 수 있습니다.
- 전체 장애유형과 비장애를 함께 선택할 수 있습니다. 개별 장애유형을 선택하면 전체 장애유형만 해제하며, 비장애는 유지합니다.
- 우선순위 문항에 순위 수를 설정할 수 있습니다. 복수선택 문항은 자유 선택·모두 선택·정해진 개수 선택을 지원합니다. 선택 문항은 무응답을 허용하고, 답변한 경우 지정 개수를 확인합니다. 기존 문항의 선택 방식은 유지합니다.
- 추가 설문의 첫 페이지에서 신청정보·동의로 돌아갈 수 있습니다. 기본정보·첨부와 작성한 설문 답변을 유지하며, 돌아와 수정한 기본정보를 다음 제출에 사용합니다.
- 기본 신청 이름과 전자서명이 다르면 진행을 차단합니다. 설문 서버도 서명과 선택 개수를 저장 전에 확인합니다. 이름 양끝 공백과 유니코드 정규화만 허용하고, 다른 이름은 허용하지 않습니다.

## 운영 반영 (2026-09-11)

- 웹 v94 운영 배포: `6aa3dac0a0fd7f9e7a555c9a`.
- 사이트: https://incheon-nurimmap.netlify.app/
- Supabase `nurim-survey` 함수 v12 배포 완료. JWT 검증 유지.
- `swift-processor` v30 및 Apps Script v91은 변경하지 않았습니다. DB 변경도 없습니다.
- 서버 배포 파일: `backend/nurim-survey.ts`, 루트 `survey-core.mjs`, 기존 `storage-policy.mjs`.

## 검증

- 자동 검사 12개 파일 통과. 순위·복수선택 규칙, 기존 설문 호환성, 잘못된 서명 및 선택 개수의 서버 저장 전 차단 확인.
- 로컬 및 실제 운영 웹 자산을 사용한 브라우저 검사 통과. API는 모의 응답으로 처리하여 운영 신청 자료를 만들지 않았습니다.
- 담당자 전환·양식 초기화, 최고관리자 입력, 장애유형 조합, 서명 불일치, 이전 단계 복귀·답변 보존, 390px 모바일 가로 넘침 검사.
- 실제 운영 Google Drive로 신청을 전송하는 검사는 수행하지 않았습니다.

## 작업 및 복구

- 최신 작업 브랜치는 `codex/survey-integration`입니다. `main`은 v77 복구 기준입니다.
- `node --test tests/*.test.mjs`, `node scripts/build.cjs`로 검증·빌드합니다.
- 브라우저 검사는 Playwright와 Edge가 있는 환경에서 `node scripts/browser-v94.cjs`로 실행합니다. 필요시 `PLAYWRIGHT_MODULE`로 모듈 경로를 지정합니다. `NURIM_TEST_URL`을 지정하면 해당 배포의 화면을 사용하되 Supabase 요청은 모의 응답으로 처리합니다.
- 이전 웹 v93 배포: `6aa1f618655a7ca315814286`. 이전 서버 함수: `nurim-survey` v11.
- 새로운 선택 개수 규칙을 사용하는 설문을 게시한 뒤에는 이전 서버로 단순 복구하면 검증이 약해질 수 있습니다. 해당 규칙을 유지하는 수정본을 사용하세요.
