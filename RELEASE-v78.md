# 누림지도 설문 통합 v78 — 적용 및 확인
작성일: 2026-09-08

## 적용 상태
- Supabase: 설문 정의·수정 이력·접수 확인번호 테이블, 담당자 권한 검사, 자동 프로그램 주소 필드 적용 완료.
- 설문 전용 Edge Function `nurim-survey`: JWT 검증을 켜고 운영·정식 미리보기 도메인만 허용. 담당자 토큰은 서버에서 추가 검증.
- 기존 `swift-processor`: 운영 버전 그대로 유지. 교체하지 않음.
- 웹: v78 패키지 준비본. Netlify 파일 업로드 인증 승인을 기다리는 중이며, 운영 웹은 아직 v77.
- Google Apps Script: v78 파일 준비 완료. 사용자가 기존 웹 앱 배포를 새 버전으로 업데이트해야 함.
- GitHub: 비공개 저장소 접근 권한 확인. 기존 운영 복구 기준과 설문 통합 작업 브랜치를 업로드 중. 업로드 과정에서 발견한 기존 로컬 데모 로그인 기본값을 제거했으며 운영 Supabase 인증은 유지.
- 기존 프로그램 19개와 신청 기록 20개 삭제 완료. 관리자 프로필 10개 유지. 복구 백업은 작업공간의 archive/pre-v78에 별도 보관하며 저장소·배포 ZIP에 포함하지 않음.

## 사용 흐름
1. 담당자 로그인 → 설문 관리 → 새 설문 만들기.
2. 제목·안내·문항·선택지를 편집하고 '수정 내용 저장'.
3. 'Google 연결 점검' → '저장본 Google 배포 / 업데이트'.
4. 프로그램 등록의 신청정보 설정에서 배포한 설문을 연결하고 최종 저장.
5. 신청 버튼을 누르면 같은 화면의 iframe 안에서 설문을 작성. 주소만 프로그램 고유 주소로 변경.
6. 설문 관리에서 해당 설문을 열어 응답·그래프·CSV 확인. 수정 이력별 응답을 선택해 조회.

## 데이터 저장 구분
- 누림 DB: 이름·생년월일·연락처, 프로그램/문항 버전 식별자, 접수 상태·순번·처리 시각, Google 저장 상태 및 링크.
- 문항 정의와 저장자 이력은 누림 DB에도 보존. 실제 상세 응답·개별 동의 답변·상세 출력 신청서는 Google에만 저장.
- 응답 목록은 담당자가 요청할 때 Google에서 읽으며, 웹 DB 및 localStorage에 상세 답변을 복사하지 않음.
- 전송 실패 복구를 위해 브라우저 sessionStorage에는 무작위 신청번호·확인 토큰만 보관. 답변은 보관하지 않으므로 새로고침 후에는 다시 입력해야 함.
- Google 저장이 끝나야 신청 완료 안내. 실패 시 완료로 안내하지 않으며, 동일 확인번호로 다시 전송 가능.
- Google 저장 이전에 예약된 기본정보가 남을 수 있음. 신청 창을 닫아 다시 입력할 수 없는 경우 담당자가 보류 건을 확인하고 취소한 뒤 재접수를 안내.

주의: 위 개인정보 분리 규칙은 '누림 설문 연결'을 사용한 신청에 적용됨. 호환성을 위해 '기존 신청 방식 사용'은 유지되어 있으며, 기존 Google Form·파일 업로드 방식은 v77 저장 경로를 사용한다. 모든 접수를 새 방식으로 강제하는 변경은 기존 기능 영향 때문에 자동 승인 검토에서 차단되어, 사용자 선택을 기다리는 중이다.

## Google Drive 구조
기관/담당자 설정 폴더/
  설문_<고유번호>/
    빈설문지_vN.pdf (최신 배포본)
    설문응답 (Google 스프레드시트: v1, v2 등 문항별 탭)
    전체신청명단.csv (모든 문항 버전의 기본 명단)
    이전버전/ (이전 빈 설문지 PDF, 버전별 문항 JSON)
    출력원본/ (편집 가능한 Google 문서)
    프로그램_<고유번호>/작성된신청서/
      신청서_<접수번호>.pdf
      이전버전/ (기본정보 수정 전 PDF)
      출력원본/

CSV 생년월일은 YYYY-MM-DD 한 칸. 인쇄본은 YYYY년 MM월 DD일. 상세 응답 CSV는 설문 관리에서 문항 버전별로 내려받는다.
짧은 문항은 두 칸, 긴 질문·설명·답변은 전체 폭으로 출력한다. Google PDF는 Google 문서의 자동 줄바꿈·페이지 나눔을 사용한다. 담당자 확인란은 넣지 않았다.
Google PDF 실제 글꼴·페이지 배치는 Apps Script 업데이트 후 실문서로 추가 확인해야 한다.

## 프로그램 주소
형식: /고유번호/생성연도/복지관명
예: /1/2026/부평장애인종합복지관
고유번호는 DB에서 생성한다. 생성 연도와 생성 당시 복지관명은 수정해도 고정된다.
신청 버튼은 history API로 주소를 바꾸며 페이지를 새로 이동하지 않는다. 직접 주소 열기·뒤로가기를 지원한다.
번호는 삭제되거나 실패한 생성 시도로 인해 연속되지 않을 수 있다.

## 검증한 항목
- Chrome에서 날짜 선택 1회, 설문 문항 편집, iframe 미리보기 로드, JavaScript 오류 검사.
- 윤년·잘못된 날짜, 우선순위 중복·변조, 필수 누락, 미동의 허용, CSV 수식 주입 방지, 인쇄 HTML 이스케이프.
- DB 트랜잭션에서 버전 저장 충돌, 프로그램 주소 불변성, 중복 접수 방지, 상세 응답 저장 거부, 비공개 테이블 접근 권한.
- 배포된 서버의 비로그인 관리 요청·잘못된 신청번호 거부.
- DB 검증용 데이터는 전부 rollback하여 남기지 않았다.

## 아직 필요한 실제 확인
1. GitHub 저장소 접근 허용 및 Netlify CLI 로그인 승인.
2. backend/APPS-SCRIPT-SETUP.md 순서로 Apps Script 적용.
3. 실제 담당자 계정으로 설문 저장·배포·연결.
4. 식별 가능한 테스트 정보로 한 건 제출 → Google 시트 1행, 개별 PDF, CSV 생성 확인.
5. 같은 제출 재시도 → 중복 행 없음 확인.
6. 문항 수정·재배포 → 기존 응답 탭 유지, 새 응답은 새 탭 확인.
7. 기본정보 수정·취소 → Google 원본·CSV·최신 PDF 확인.
8. 담당자 두 계정으로 상대방 설문 접근 거부 확인.
이 단계 전에는 Google 저장까지의 실서비스 전체 검증이 완료됐다고 볼 수 없다.

## 추가 기능 및 끄기
feature-config.js의 charts:false는 그래프 표시를, programAddress:false는 신청 버튼의 주소 변경을 끈다.
survey:false는 설문 관리 메뉴를 숨긴다. 이미 연결된 신청서는 계속 동작하도록 유지한다.
문항 수정 충돌 방지, 재시도 확인번호, 동의/미동의 구분, CSV 안전 처리는 데이터 보호·정합성 기능이므로 임의 제거 대상이 아니다.

## 롤백
- 로컬 Git 태그 rollback-v77은 변경 전 정적 웹을 가리킨다.
- 운영 기준 배포: https://6a98feadae91a72a6ee9982d--incheon-nurimmap.netlify.app
- Netlify에서 위 배포를 재게시하면 기존 화면으로 돌아간다. GitHub 연결 후에는 기록된 안정 커밋도 이용할 수 있다.
- 이미 새 설문을 연결한 프로그램은 웹 롤백 전에 survey_id 연결을 검토해 해제해야 기존 접수 화면과 DB 가드가 충돌하지 않는다. 응답·문항 테이블은 삭제하지 않는다.
- 프로그램/신청자 데이터 복원은 별도 백업을 이용한 관리자 작업이며, 웹 롤백과 자동으로 같이 복원되지 않는다.
- 자동으로 모든 런타임 오류를 탐지해 되돌리는 기능은 구현하지 않았다. GitHub 검사는 코드 검사 실패를 확인하는 용도이며, 배포 후 문제는 검증된 이전 배포로 복구한다.

